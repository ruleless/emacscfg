SUBT:=copyt

TOPDIR:=../../..
DESTDIR:=$(HOME)/.emacs.d/snippets/c++-mode
SRCFILES:=$(wildcard *)
SRCFILES:=$(filter-out Makefile,$(SRCFILES))
SRCFILES:=$(filter-out Makefile~,$(SRCFILES))

include $(TOPDIR)/build.mak
